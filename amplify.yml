version: 1
applications:
  - appRoot: mullai-periyar-sangam
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 22
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
